# For CPU-only training:
# torch>=2.0.0
# torchvision>=0.15.0

# For GPU training (CUDA 12.1):
# Install with: pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
torch>=2.0.0
torchvision>=0.15.0

transformers>=4.30.0
pytorch-lightning>=2.0.0
pandas>=1.5.0
numpy>=1.24.0
scikit-learn>=1.3.0
test-tube>=0.7.5
matplotlib>=3.7.0
seaborn>=0.12.0
tqdm>=4.65.0

# Note: On systems with PEP 668 (externally managed Python),
# use: pip install --break-system-packages -r requirements.txt
