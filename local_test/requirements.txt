# ============================================================
# Module A: Sentence-Level Bias & Emotion Engine
# RunPod Template: PyTorch 2.x + CUDA 12.x (pre-installed)
#
# Install with:
#   pip install -r local_test/requirements.txt
# ============================================================

# --- Core ML (PyTorch is pre-installed on RunPod pods) ---
# Uncomment the line below ONLY if running outside RunPod:
# torch>=2.1.0

# --- NLP ---
transformers>=4.36.0
tokenizers>=0.15.0

# --- Data ---
pandas>=2.0.0
numpy>=1.24.0
scikit-learn>=1.3.0

# --- Visualization & Logging ---
matplotlib>=3.7.0
seaborn>=0.12.0
tensorboard>=2.14.0

# --- Progress bars ---
tqdm>=4.65.0
